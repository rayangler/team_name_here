<h2>Users</h2>

<ol>
{{#each renderData.notfollowing}}
  <li><a href = 'profile/{{id}}'>{{username}}</a><form action="/add_follower" method="POST"><input type="hidden"
  name="followinguserid" value="{{id}}"><input type="submit" value="Follow"></form></li>
{{/each}}
</ol>

<h2>Following</h2>

<ol>
{{#each renderData.following}}
  <li><a href = 'profile/{{id}}'>{{username}}</a><form action="/delete_follower" method="POST"><input type="hidden"
  name="followinguserid" value="{{id}}"><input type="submit"
  value="Remove Follower"></form></li>
{{/each}}
</ol>
